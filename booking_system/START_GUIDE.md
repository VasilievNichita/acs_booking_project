# 🚀 Полная инструкция по запуску TravelNv

## Шаг 1: Запуск Backend (Spring Boot)

### Вариант 1: Через Maven (рекомендуется)

Откройте терминал в **корневой папке проекта** (где находится `pom.xml`):

```bash
cd "c:\Users\User\Desktop\utm\3 god 1 sem\ACS\booking_system"
mvn spring-boot:run
```

### Вариант 2: Через IDE

1. Откройте `BookingSystemApplication.java`
2. Нажмите правую кнопку мыши → Run

### ✅ Проверка Backend:

Backend должен запуститься на **http://localhost:8080**

Откройте в браузере: http://localhost:8080/api/apartments/available

Если видите JSON с квартирами или пустой массив `[]` - backend работает!

---

## Шаг 2: Запуск Frontend (React + Vite)

Откройте **НОВЫЙ** терминал в папке `client`:

```bash
cd "c:\Users\User\Desktop\utm\3 god 1 sem\ACS\booking_system\client"
npm install
npm run dev
```

### ✅ Проверка Frontend:

Frontend откроется на **http://localhost:3000**

---

## Шаг 3: Тестирование подключения

### Способ 1: Тестовая страница

Откройте в браузере: **http://localhost:3000/test**

Нажмите кнопки для тестирования:
- "Проверить подключение к Backend"
- "Тест регистрации пользователя"

### Способ 2: Консоль браузера

1. Откройте http://localhost:3000
2. Нажмите **F12** (откроется DevTools)
3. Перейдите на вкладку **Console**
4. Кликайте по элементам на странице
5. Вы должны видеть сообщения типа:
   - "Открыть квартиру: 1"
   - "Избранное: true"
   - "Регистрация"

### Способ 3: Network вкладка

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу (F5)
4. Вы должны увидеть запрос к `/api/apartments/available`
5. Статус должен быть **200 OK**

---

## 🔧 Решение проблем

### Проблема: "Cannot connect to backend"

**Решение:**
1. Проверьте, что backend запущен (должен быть на порту 8080)
2. Откройте http://localhost:8080/api/apartments/available напрямую
3. Если не открывается - перезапустите backend

### Проблема: "Port 3000 is already in use"

**Решение:**
Vite автоматически выберет другой порт (3001, 3002 и т.д.)
Используйте тот порт, который показан в терминале

### Проблема: "npm: command not found"

**Решение:**
Установите Node.js с https://nodejs.org/

### Проблема: Кнопки не работают

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте, есть ли ошибки
3. Убедитесь, что вы перезапустили frontend после изменений

### Проблема: CORS ошибки

**Решение:**
Я уже исправил SecurityConfig.java - перезапустите backend:
```bash
# Остановите backend (Ctrl+C)
# Запустите снова
mvn spring-boot:run
```

---

## 📋 Чеклист запуска

- [ ] Backend запущен на http://localhost:8080
- [ ] Frontend запущен на http://localhost:3000
- [ ] Открыл http://localhost:3000/test
- [ ] Тесты проходят успешно
- [ ] В консоли (F12) нет красных ошибок
- [ ] Кнопки реагируют на клики

---

## 🎯 Что должно работать

### На главной странице (http://localhost:3000):
- ✅ Отображается логотип "TravelNv"
- ✅ Показываются категории "Жилье" и "Впечатления"
- ✅ Загружаются карточки квартир из backend
- ✅ Кнопка сердечка меняет цвет при клике
- ✅ Меню пользователя открывается/закрывается
- ✅ Все клики выводятся в консоль

### На тестовой странице (http://localhost:3000/test):
- ✅ Кнопка "Проверить подключение" показывает список квартир
- ✅ Кнопка "Тест регистрации" создает пользователя

---

## 💡 Полезные команды

### Backend:
```bash
# Запуск
mvn spring-boot:run

# Остановка
Ctrl + C

# Очистка и пересборка
mvn clean install
```

### Frontend:
```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Остановка
Ctrl + C
```

---

## 📞 Если ничего не помогло

1. Закройте все терминалы
2. Перезапустите IDE
3. Запустите backend заново
4. Запустите frontend заново
5. Откройте http://localhost:3000/test
6. Проверьте консоль браузера (F12)
